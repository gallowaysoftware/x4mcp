# Lifecycle scripts never run on install: a UI build step must not be able to
# execute arbitrary code on a machine that only wanted to change a stylesheet.
ignore-scripts=true
# Exact versions on `npm install <pkg>` — the built dist/ is committed and CI
# diffs it, so a floating minor is a mystery rebuild waiting to happen.
save-exact=true
